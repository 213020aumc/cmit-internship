// import { cpus } from "os";
// const logMessage = require("./logger");
import { logMessage } from "./logger.js";

// console.log(`Hello! Our Server has ${cpus().length} CPU cores.`);
// logMessage("Server started!");
logMessage("Server started!");
